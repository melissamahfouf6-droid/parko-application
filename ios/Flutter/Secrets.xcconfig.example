# Copy this file to Secrets.xcconfig in the same folder (Secrets.xcconfig is gitignored).
# The real key must be the LAST assignment to GOOGLE_MAPS_API_KEY so it overrides the placeholder in Debug/Release.xcconfig.
#
# GOOGLE_MAPS_API_KEY=paste_your_ios_maps_sdk_key_here
