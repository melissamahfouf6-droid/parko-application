apiVersion: v1
kind: Service
metadata:
  name: parko-api
  namespace: parko
  labels:
    app.kubernetes.io/name: parko-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: parko-api
  ports:
    - name: http
      port: 80
      targetPort: http
